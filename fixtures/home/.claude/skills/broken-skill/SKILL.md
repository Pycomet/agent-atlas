---
name: broken-skill
description: this frontmatter never closes

# Broken skill

The closing delimiter above is missing, so the frontmatter is malformed.
The scanner must include this item with a null description and a flag,
never crash.
