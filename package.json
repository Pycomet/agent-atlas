{
  "name": "agent-atlas",
  "version": "0.1.0",
  "description": "Scans your AI coding setup and turns it into an interactive mind map. Read-only, local-only.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "agent-atlas": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc && mkdir -p dist/renderer/assets && cp src/renderer/assets/app.js src/renderer/assets/style.css dist/renderer/assets/",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.112.4",
    "commander": "^12.1.0",
    "d3": "^7.9.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
